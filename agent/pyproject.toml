[project]
name = "agent"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "livekit-agents[assemblyai,cartesia,openai,silero,turn-detector]~=1.3",
    "livekit-plugins-groq>=1.5.11",
    "livekit-plugins-noise-cancellation~=0.2",
    "python-dotenv>=1.2.2",
]
